import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'granp.app.professional',
  appName: 'granp-professional-app',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
